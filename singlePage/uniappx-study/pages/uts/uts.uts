import { Item, PaperType } from "@/assets/type/PageInstruction";

const data : Item[] = [
	{
		name: "数据类型",
		page: "doc-type",
		summarize: [
			{ title: "推导规则:通过数字字面量以及数字字面量组成的运算表达式来给变量赋值，此时变量类型默认推导为 number类型", type: "primary" },
			{ title: "推导规则:纯数字字面量的除法，其结果会变成number", type: "primary" },
			{ title: "不能将 null 赋值给 any 类型的变量", type: "primary" },
			{ title: "如果要定义一个空数组，需要指定类型", type: "primary" },
			{ title: "JSON.parse的对象，不支持用.访问", type: "primary" },
			{ title: "", type: "primary" },
		]
	},
];

const paperTile : string = "uts"

export const utsData : PaperType = {
	paperTile: paperTile,
	data: data
};