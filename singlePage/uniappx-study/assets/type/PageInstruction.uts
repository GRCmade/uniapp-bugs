export type SummarizeItem = {
	title : string;
	type : string;
};

export type Item = {
	name : string;
	page : string;
	summarize : SummarizeItem[];
};

export type PaperType = {
	paperTile:string
	data:Item[]
}